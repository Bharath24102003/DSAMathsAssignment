vector<int> count_divisors(vector<int>& arr) {
    vector<int> result;
    for (int num : arr) {
        int count = 0;
        for (int i = 1; i <= num; i++) {
            if (num % i == 0) {
                count++;
            }
        }
        result.push_back(count);
    }
    return result;
}
int main(){
    vector<int> arr;
    for(int i=0;i<n;i++)
    {
    int a;
        cin>>a;
        arr.push_back(a);
    }
    return count_divisors(arr);
}
